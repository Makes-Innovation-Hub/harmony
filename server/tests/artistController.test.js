import { expect } from "chai";
import {
  getArtists,
  createArtist,
  getOrCreateArtist,
} from "../controllers/artistsController";

describe("Artest Controller", () => {
  describe("getArtists", () => {
    it("should return artists array if found", async () => {});
  });
});
